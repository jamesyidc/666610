# 📱 支撑/压力线 Telegram 信号推送系统说明

## 📊 信号分级体系（4种信号）

### 🎯 信号分类和触发条件

#### 1️⃣ **强势抄底信号** 🟢🟢🟢 (最强)
**触发条件（AND关系）：**
```
✅ 支撑线1达标: 触碰币种数 >= 8
✅ 支撑线2达标: 触碰币种数 >= 8
✅ 两条线同时满足
```

**特征：**
- 市场整体性强劲反弹机会
- 大量币种同时接近支撑位
- 是最强的抄底信号

**消息示例：**
```
✅✅✅ 【强势抄底信号！】 ✅✅✅
━━━━━━━━━━━━━━━━━━━━━━
💎 市场机会显现！建议关注！
━━━━━━━━━━━━━━━━━━━━━━

⏰ 触发时间: 2025-12-24 14:04:11
📊 双线同步达标：
   ✅ 支撑线1: 20个币种 (>=8)
   ✅ 支撑线2: 9个币种 (>=8)
   📈 总触碰数: 29个币种

🔥 信号强度: 🟢🟢🟢 强势买入 🟢🟢🟢
```

**代码位置：** `telegram_notifier.py` 第430-444行

---

#### 2️⃣ **双重抄底信号** 🟢🟢
**触发条件：**
```
✅ 单个币种同时触发 alert_scenario_1=1 AND alert_scenario_2=1
✅ 触发币种数 >= min_coins_double_buy (默认1)
⚠️  未达到强势信号阈值（<8个币种）
```

**特征：**
- 个别币种的双重支撑
- 该币种同时接近支撑1和支撑2两条线
- 信号强度：极强

**消息示例：**
```
🟢🟢 【双重抄底信号】 🟢🟢

⏰ 时间: 2025-12-24 14:14:02
📊 触发币种: 7个
🔥 信号强度: 极强（同时触发支撑1+支撑2）

币种列表:
1. BNBUSDT - $836.20
   距支撑1: 0.08% | 距支撑2: 0.08%
2. CFXUSDT - $0.07
   距支撑1: 0.16% | 距支撑2: 0.16%
...

💡 重要提示: 
   这些币种同时接近两条支撑线，是更强的抄底信号！
```

**代码位置：** `telegram_notifier.py` 第304-312行, 第415-426行

---

#### 3️⃣ **强势逃顶信号** 🔴🔴🔴 (最强)
**触发条件（AND关系）：**
```
✅ 压力线1达标: 触碰币种数 >= 8
✅ 压力线2达标: 触碰币种数 >= 8
✅ 两条线同时满足
```

**特征：**
- 市场整体性见顶风险
- 大量币种同时接近压力位
- 是最强的逃顶信号

**代码位置：** `telegram_notifier.py` 第463-477行

---

#### 4️⃣ **双重见顶信号** 🔴🔴
**触发条件：**
```
✅ 单个币种同时触发 alert_scenario_3=1 AND alert_scenario_4=1
✅ 触发币种数 >= min_coins_double_sell (默认1)
⚠️  未达到强势信号阈值（<8个币种）
```

**特征：**
- 个别币种的双重压力
- 该币种同时接近压力1和压力2两条线
- 信号强度：极强

**代码位置：** `telegram_notifier.py` 第340-348行, 第447-456行

---

## ⚙️ 配置参数说明

### 📋 当前配置文件
位置: `telegram_config.py`

### 🔧 关键参数

#### 1. 信号类型配置 (`signal_types`)
```python
"signal_types": {
    "buy": {
        "enabled": True,      # 强势抄底信号开关
        "min_coins": 8        # 支撑1和支撑2都需要>=8个币种
    },
    "sell": {
        "enabled": True,      # 强势逃顶信号开关
        "min_coins": 8        # 压力1和压力2都需要>=8个币种
    },
    "double_buy": {
        "enabled": True,      # 双重抄底信号开关
        "min_coins": 1        # 最少触发币种数
    },
    "double_sell": {
        "enabled": True,      # 双重见顶信号开关
        "min_coins": 1        # 最少触发币种数
    }
}
```

#### 2. 推送条件配置 (`push_conditions`)
```python
"push_conditions": {
    "min_coins": 8,              # 强势信号默认阈值
    "cooldown_seconds": 300,     # 冷却时间(秒)
    "max_retries": 3,            # 最大重试次数
    "retry_delay": 5             # 重试延迟(秒)
}
```

---

## 🔄 信号判断逻辑流程

### 抄底信号判断流程
```
1. 获取最新数据
   ↓
2. 统计支撑线触碰数
   - support_s1_count (alert_scenario_1=1的币种数)
   - support_s2_count (alert_scenario_2=1的币种数)
   ↓
3. 检查双重抄底（优先）
   if (币种同时触发s1和s2) and (count >= min_coins_double_buy):
       → 发送【双重抄底信号】
   ↓
4. 检查强势抄底
   if (s1_count >= 8) and (s2_count >= 8):
       → 发送【强势抄底信号】
   else:
       → 不发送
```

### 逃顶信号判断流程
```
1. 获取最新数据
   ↓
2. 统计压力线触碰数
   - pressure_r1_count (alert_scenario_3=1的币种数)
   - pressure_r2_count (alert_scenario_4=1的币种数)
   ↓
3. 检查双重见顶（优先）
   if (币种同时触发s3和s4) and (count >= min_coins_double_sell):
       → 发送【双重见顶信号】
   ↓
4. 检查强势逃顶
   if (r1_count >= 8) and (r2_count >= 8):
       → 发送【强势逃顶信号】
   else:
       → 不发送
```

---

## 📈 实际案例分析

### 案例1：强势抄底信号
```
情况：
- 支撑线1: 20个币种触碰
- 支撑线2: 9个币种触碰

判断结果：
✅ 触发【强势抄底信号】
因为: 20 >= 8 AND 9 >= 8

推送内容：
- 显示支撑1和支撑2的数量
- 列出单独触发的币种
- 不包括双重触发的币种
```

### 案例2：双重抄底信号
```
情况：
- 7个币种同时触碰支撑1和支撑2
- 支撑1总数: 12个（包括这7个）
- 支撑2总数: 7个（就是这7个）

判断结果：
✅ 触发【双重抄底信号】(7个币种)
❌ 不触发【强势抄底信号】(因为支撑2只有7个 < 8)

推送内容：
- 显示7个币种列表
- 每个币种显示到支撑1和支撑2的距离
```

### 案例3：同时触发两种信号
```
情况：
- 10个币种同时触碰支撑1和支撑2
- 另有5个币种只触碰支撑1
- 另有3个币种只触碰支撑2

统计：
- support_s1_count = 10 + 5 = 15
- support_s2_count = 10 + 3 = 13
- double_buy = 10

判断结果：
✅ 触发【双重抄底信号】(10个币种) - 优先推送
✅ 触发【强势抄底信号】(s1=15, s2=13) - 之后推送（冷却期后）

推送顺序：
1. 先推送双重抄底（更强信号）
2. 等待冷却期（5分钟）
3. 再推送强势抄底
```

---

## 🎯 关键区别总结

| 特征 | 强势信号 | 双重信号 |
|------|---------|---------|
| **判断维度** | 市场整体 | 个别币种 |
| **触发条件** | 两条线都>=8个币种 | 单币种同时触碰两条线 |
| **信号强度** | 最强（整体机会） | 极强（个股机会） |
| **推送优先级** | 较低 | 较高（先推送） |
| **阈值设置** | 固定8个 | 可配置(默认1) |
| **应用场景** | 大盘趋势判断 | 精准个股操作 |

---

## ✅ 配置建议

### 推荐配置
```python
"signal_types": {
    "buy": {
        "enabled": True,
        "min_coins": 8
    },
    "sell": {
        "enabled": True,
        "min_coins": 8
    },
    "double_buy": {
        "enabled": True,
        "min_coins": 3        # 建议>=3个才推送
    },
    "double_sell": {
        "enabled": True,
        "min_coins": 3
    }
}

"push_conditions": {
    "min_coins": 8,
    "cooldown_seconds": 300   # 5分钟冷却
}
```

### 配置说明
- **强势信号阈值(8)**: 确保只在市场整体信号明显时推送
- **双重信号阈值(3)**: 避免单个币种误触发，要求多个确认
- **冷却时间(300秒)**: 避免频繁推送相同信号

---

## 🔧 维护和调试

### 查看实时日志
```bash
pm2 logs telegram-notifier --lines 50
```

### 重启服务
```bash
pm2 restart telegram-notifier
```

### 测试信号检测
```bash
cd /home/user/webapp
python3 telegram_notifier.py
```

---

## 📞 相关文件
- **主程序**: `telegram_notifier.py`
- **配置文件**: `telegram_config.py`
- **数据库**: `databases/support_resistance.db`
- **日志文件**: `logs/telegram-notifier-out.log`

---

**更新时间**: 2025-12-24  
**文档版本**: v1.0
